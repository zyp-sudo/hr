【运行顺序】
必须等待 Agent1 完成后再运行。你是 DeepSeek Agent2，负责独立终验；发现问题时直接做最小修复，然后重新完整验证。不要只写审查报告。

【项目位置】
E:\202676\talentmatch

【任务背景】
Agent1 已负责把未登录主页的 Hero 视觉、出现动效、整页前景环绕和加载反馈与登录后主页同步，同时保留游客权限边界。你必须基于最终代码与实际页面验证，不要相信 Agent1 的自述。

【重点终验清单】
1. 未登录 Hero 与登录后 Hero 的背景、尺寸、标题三段式、左右图片、装饰粒子和按钮布局是否属于同一视觉体系。
2. 游客 Hero 是否真实挂载 useHomeHeroMotion，而非只复制静态图片。
3. 轨迹是否从右向左经过标题后方，再从左侧穿出并在标题前方形成包围关系。
4. 动画结束瞬间是否跳位、闪白、透明度突变或整体消失。
5. 最终轨迹、符号、粒子是否常驻；临时星头/拖尾是否正确隐藏。
6. React StrictMode、登录/退出切换、重复进入主页后是否出现 overlay 数量翻倍、定时器残留或动画加速。
7. 游客统计是否在慢加载时显示 skeleton，是否避免 0 值闪烁。
8. “浏览 Demo 岗位”是否滚动到正确区域；“登录查看全部数据”是否打开登录入口。
9. 游客只能查看 Demo/只读内容，不能调用登录后新增、评估、收藏、面试等受限操作。
10. 1440×900 与 390×844 均无横向溢出，移动端文字与按钮不被裁切。
11. prefers-reduced-motion 下无长动画且内容可见。
12. 控制台无 React key、重复 id、GSAP target not found、资源 404 等错误。

【动画衔接的硬性判定】
逐帧观察动画末尾。运行路径终点与常驻布局必须来自同一坐标/同一 progress 计算；如果 onComplete 又把元素 set 到另一组位置，即使肉眼跳动很小也视为不通过，必须修复。禁止通过提前淡出再淡入隐藏跳变。

【代码约束】
1. 保留用户和 Agent1 的有效改动，只修本任务缺陷。
2. 不安装依赖，不改后端，不重写整个页面，不执行 git reset/checkout。
3. cleanup 必须完整，overlay 必须 pointer-events:none。

【强制命令】
在 E:\202676\talentmatch 执行：
1. npm.cmd run lint
2. npm.cmd run build

【终验报告】
输出：
1. 结果：通过 / 修复后通过 / 不通过。
2. 实际发现的问题与对应修复文件。
3. 桌面端、移动端、登录切换、动画末帧、权限边界的逐项结果。
4. lint/build 结果。
5. 仍存在的风险，不得隐瞒。

