【角色】
你是 DeepSeek Agent1，资深 React / TypeScript / GSAP 前端工程师。你负责直接修改项目代码，不只给建议。

【项目位置】
E:\202676\talentmatch-frontend

【本轮目标】
完成“未登录（游客态）主页”的视觉设计同步：该同步的视觉、动效、加载反馈与响应式行为要和登录后主页一致；游客数据、登录入口和权限限制必须保持游客态，绝不能把登录后的业务操作直接开放出来。

当前工作区可能已经存在一版未完成的登录前 Hero 同步骨架。先逐行检查现状，在现有实现上修正和收尾；不要回滚、覆盖或格式化与本任务无关的用户改动。

【先检查这些文件】
1. E:\202676\talentmatch-frontend\src\PlatformPages.tsx
2. E:\202676\talentmatch-frontend\src\hooks\useHomeHeroMotion.ts
3. E:\202676\talentmatch-frontend\src\reference-theme.css
4. E:\202676\talentmatch-frontend\src\components\SplitText.tsx
5. E:\202676\talentmatch-frontend\public\hero\orbit-elements\

【必须实现】

一、同步登录前 Hero 的视觉骨架
1. 未登录 Hero 使用与登录后相同的 home-hero--showcase 尺寸、背景、留白和层级体系，不再是 300px 的简化横幅。
2. 未登录 Hero 必须挂载 heroRef，使 useHomeHeroMotion 能找到该 Hero。
3. 同步环境粒子、左侧岗位管理图片、右侧招聘洞察图片、左右轨迹装饰；图片沿用：
   - /hero/job-management.png
   - /hero/recruitment-insights.png
4. 文字中心层必须处于正确的前后景层级：后景轨迹可以从标题后穿过，前景轨迹可以从标题前穿过，但按钮必须始终可点击。
5. 不允许出现重复 SVG id 冲突、重复 overlay、重复监听器或每次渲染不断追加 DOM 的问题。

二、同步标题和出现动效
1. 标题仍为三段：人岗匹配 / 智能 / 评估系统，其中“智能”保留强调色。
2. 使用与登录后相同的 SplitText 分段出现节奏，标题与两行描述上下动态出现，不能突然闪现。
3. 游客态文案保持游客语义，例如：
   - 登录后可查看完整岗位数据、启动人才评估、使用知识图谱与招聘趋势等全部功能。
   - 当前开放 10 条精选 Demo 岗位，登录即可解锁完整招聘工作台。
4. 按钮保持两个：
   - 主按钮“登录查看全部数据”，触发 auth:open-login。
   - 次按钮“浏览 Demo 岗位”，平滑滚动到 Demo 岗位区。

三、让整页前景环绕动效在游客态正常运行
1. 登录前进入主页时，也要运行现有的整页前景轨迹/符号动画；不要再用 isLoggedIn 直接阻断动画。
2. 动效视觉要求：轨迹整体先在页面前景从右向左运动，经过“人岗匹配 / 智能 / 评估系统”文字后方，再从左侧穿出并包围标题。
3. 最终环绕效果必须常驻页面，不能在结束时消失。
4. 必须修复“最后一帧没有衔接/结束瞬移”：
   - 动画运行态与最终常驻态共用同一套路径坐标和最终 progress 定义。
   - 不允许在 onComplete 中把元素从动画终点突然 set 到另一组坐标。
   - 最后一帧与常驻第一帧的 x、y、rotation、scale、opacity 必须连续。
   - 如需切换 class，只能切换不会改变几何位置的视觉状态。
5. 动画结束后只隐藏临时星头/临时拖尾；组成最终环绕的轨迹、符号和粒子继续显示。
6. effect cleanup 必须 kill timeline、移除本次创建的 overlay 和监听器；React StrictMode 下不得叠加两套动画。

四、同步加载反馈，但不越权同步业务内容
1. 未登录统计卡加载超过现有延迟阈值时，使用与登录后相同的 skeleton；数据返回后平滑显示真实 Demo 统计，不能先闪 0。
2. Demo 岗位区增加稳定锚点（例如 id="demo-jobs"），供次按钮滚动。
3. 继续保留：Demo 岗位、登录 CTA、平台能力预览、只读岗位详情。
4. 不要把登录后的“新增岗位、候选人、面试、收藏、招聘趋势操作”等权限复制到游客态。

五、响应式和无障碍
1. 桌面端 1440×900：左右图片、标题、前后景轨迹层级正确，无横向滚动条。
2. 移动端 390×844：不横向溢出；可按现有 CSS 隐藏复杂侧图/环绕轨迹，但标题、文案和两个按钮必须完整可用。
3. prefers-reduced-motion 下跳过长动画，内容和最终必要视觉直接可见。
4. 装饰元素 aria-hidden；按钮保留键盘可用性；不要让 overlay 拦截鼠标事件。

【实施约束】
1. 先阅读现有实现再修改，优先复用现有 class、Hook 和资源，不另起一套动画系统。
2. 只做与本任务直接相关的最小修改；禁止重写整个 PlatformPages.tsx。
3. 不安装新依赖，不修改后端，不改接口，不清理工作区，不使用 git reset/checkout 覆盖他人改动。
4. 不要只调整 duration 掩盖衔接问题；必须从共享最终坐标/进度的根因解决。
5. 如果当前代码已有初步同步实现，要审查它是否满足以上条件并直接修正，不要重复插入相同结构。

【强制验证】
在 E:\202676\talentmatch-frontend 执行：
1. npm.cmd run lint
2. npm.cmd run build
3. 实际打开未登录主页做桌面端与移动端检查。
4. 检查控制台无报错，document.documentElement.scrollWidth <= window.innerWidth。
5. 观察完整动画直到结束，确认结束前后没有位置跳变且最终环绕常驻。
6. 再登录一次，确认登录后主页原有效果和业务按钮没有被破坏。

【完成后报告格式】
1. 修改文件清单。
2. 每个文件改了什么。
3. 动画衔接根因与实际修复方式。
4. 游客态与登录态分别验证结果。
5. lint/build 原始结果摘要。
6. 若仍有未完成项，明确说明；不得用“理论上可行”代替实际验证。

