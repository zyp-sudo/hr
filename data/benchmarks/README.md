# MVP accuracy benchmark

`accuracy_report.json` is generated by `python scripts/evaluate_accuracy.py`.
The curated regression cases protect the deterministic MVP pipeline; they do
not claim production accuracy on an independently annotated corpus.
